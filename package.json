{
  "name": "gps-web-app",
  "version": "1.0.0",
  "main": "frontend/src/App.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd frontend && npm install --force --legacy-peer-deps && npm run build",
    "dev": "cd frontend && npm start",
    "netlify-dev": "netlify dev"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  },
  "devDependencies": {
  "netlify-cli": "^15.0.0"
  }
}
